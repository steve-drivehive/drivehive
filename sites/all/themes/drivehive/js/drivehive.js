jQuery(document).ready(
        function(){
            var banner = jQuery('#banner-container');
            bannerHeight = banner.height();
            var bannerOffset = banner.offset();
            //alert(bannerOffset.left);
            //alert(bannerOffset.top);
        }
        );


